<!-- TO DELETE: run `git rm -r .cursor/` in the kit repository.
     Agent files are generated by install-agent-files.py into the project root.
     See docs/agent-instructions.md for the canonical source. -->
