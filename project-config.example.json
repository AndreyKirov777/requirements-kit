{
  "$schema": "./schema/project-config.schema.json",
  "$comment": "Copy this file to project-config.json and fill in your project-specific values. Run `python scripts/generate-fileclasses.py` after editing.",
  "domains": [
    "auth",
    "payments",
    "catalog"
  ],
  "owners": [
    "@alice",
    "@bob",
    "team-platform"
  ]
}
